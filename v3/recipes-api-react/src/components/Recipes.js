import React from 'react'

import React from 'react'

export default function Recipes(props) {
    console.log(this.props);
    
  return ({}
    // { this.props.recipes.map((recipe, i)=>{
    //     return(
    //       <div key={`recipe-${i}`} className="recipe-preview" >
    //         <img src={recipe.image_url} alt={recipe.title}/>
    //         <h3>{recipe.title}</h3>
    //       </div>
    //     )
    //   })}
  )
}

